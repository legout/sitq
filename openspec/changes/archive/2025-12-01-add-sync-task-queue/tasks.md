## 1. Implementation
- [x] 1.1 Implement `SyncTaskQueue` as a context manager that owns an internal asyncio event loop.
- [x] 1.2 Delegate `enqueue` and `get_result` to the async `TaskQueue` using the internal loop.
- [x] 1.3 Document usage constraints and examples in docstrings and the README.
- [x] 1.4 Add tests for basic synchronous enqueue/get_result flows.

